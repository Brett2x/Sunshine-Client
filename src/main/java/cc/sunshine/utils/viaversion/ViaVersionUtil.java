package cc.sunshine.utils.viaversion;

import cc.sunshine.ExampleMod;
import cc.sunshine.utils.IMinecraft;
import net.minecraft.entity.Entity;
import net.minecraft.util.Hand;

public final class ViaVersionUtil implements IMinecraft {
    /*public static void attack(Entity entity, boolean swing) {
        boolean olderThan1_9 = ExampleMod.INSTANCE.getVersion().isOlderThan(VersionEnum.r1_9);

        if(olderThan1_9 && swing) {
            mc.player.swingHand(Hand.MAIN_HAND);
        }

        mc.interactionManager.attackEntity(mc.player, entity);

        if(!olderThan1_9 && swing) {
            mc.player.swingHand(Hand.MAIN_HAND);
        }
    }*/
}
